# Memory - how-is-today development

## 2025-05-20
- Added tests for `--seed-only` CLI flag (basic, with --date, with --seed)

## 2025-01-16
- Added test for `--emoji-count` flag with `--from-date`/`--to-date` options
- Test verifies emoji count is correctly calculated for date range queries